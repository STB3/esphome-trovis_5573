api:    
  reboot_timeout: 600min

text_sensor:
  - platform: version
    name: "Version"
    hide_timestamp: true
    icon: mdi:new-box
    entity_category: diagnostic

button:
  - platform: restart
    name: Reboot

  - platform: safe_mode
    id: button_safe_mode
    name: Safe Mode Boot

  - platform: factory_reset
    id: factory_reset_btn
    name: Factory Reset

